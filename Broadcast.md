A ==one-to-all== routing method. One device sends a single packet, which is received and processed by *every other device* on that specific local network segment.

Strictly limited to the local network, because [[Routing|Router]]s generally block it to prevent network congestion.

Commonly used by network discovery protocols like [[Address Resolution Protocol]] (ARP).