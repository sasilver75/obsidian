Part of the [[HTTP]] specification and defines how to ask for a *specific byte range* from a file, rather than the entire file itself.

A core part of what allows us to make [[Cloud-Native Geospatial|Cloud-Optimized]] file formats, because it means that part of a geospatial data file can be read and used without needing to download the entire file.