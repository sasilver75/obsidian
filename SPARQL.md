Pronounced "Sparkle," it is a semantic query language and protocol used to retrieve and manipulate data stored in RDF (Resource Description Framework) format. 

It's the standard language used to query knowledge graphs, originally designed specifically for [[Resource Description Framework]] (RDF) triplets, its use has since expanded through modern integration tools and database virtualization.