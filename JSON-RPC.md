A lightweight, transport-agnostic [[Remote Procedure Call]] (RPC) protocol that uses [[JSON]] for data serialization, allowing clients to invoke methods on a server.

Focuses on calling specific actions or functions (e.g. `eth_getBalance`), rather than manipulating resources, making it simpler than [[Representational State Transfer|REST]].


