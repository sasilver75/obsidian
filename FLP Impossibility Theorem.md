Fischer, Lynch, and Paterson

A foundational result in distributed computing, proving that in a fully asynchronous network, no deterministic algorithm can ***guarantee*** consensus if even a single node is allowed to crash.