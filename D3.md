Data visualization library giving you low-level control over SVG/[[Canvas]] rendeing.

Used everywhere for charts. Not a map renderer, but often used alongside map libraries for time-series charts and detail panel components.

`Plot` is D3's higher-level successor