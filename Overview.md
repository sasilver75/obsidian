Downsampled (aggregated) data intended for visualization and stored as part of a file format.
Overviews are part of the [[Cloud-Optimized GeoTIFF]] specification, and allow reading "zoomed out" data without needing to read and sample from "full resolution" data.

Closely related to [[Tile Pyramid]]s (if not somewhat synonymous)

