---
aliases:
  - CTE
---
A temporary, named result set in SQL that you can reference within a subsequent `SELECT`, `INSERT`, `UPDATE`, or `DELETE` statement.

Simplifies complex queries, improves readability by breaking multi-step logic into modular blocks, and allows for recursive operations.

![[Pasted image 20260713174237.png]]

