[[Roboflow]]'s model that leverages the [[DINOv2]] backbone, and uses it in a realtime object detection context.

Tries to answer the question: Why aren't we leverage big pretraining for visual models?


